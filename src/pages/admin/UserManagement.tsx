// Deprecated and unused — replaced by StaffManagement.tsx (see
// src/pages/admin/StaffManagement.tsx), which is reachable directly from the
// sidebar as "Staff Management" (Super Admin only) instead of being nested
// inside Admin Tools. Left empty rather than deleted outright (file deletion
// is blocked in this environment) — safe to `git rm` this file yourself.
export {};
