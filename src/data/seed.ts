// Deprecated and unused — real auth now goes entirely through Supabase Auth
// (see src/services/supabaseService.ts). This file used to hold a hardcoded
// demo user list with real staff usernames and plaintext PINs; it was dead
// code (nothing imported it) but had already been committed to git history.
// Left empty rather than deleted outright (file deletion is blocked in this
// environment) — safe to `git rm` this file yourself.
export {};
