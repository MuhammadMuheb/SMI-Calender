/**
 * Placeholder for the retired future-cycle manager. Only real, user-submitted
 * requests for the current cycle are shown now, so there is nothing to render.
 * Kept because AppShell still mounts it; safe to delete together with that usage.
 */
export default function CycleManager() {
  return null;
}
